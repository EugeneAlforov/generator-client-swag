import {

} from './my-slice.actions';

const initialState = {

};


export default function mySliceReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
