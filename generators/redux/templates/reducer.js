import {

} from './<%= fileName %>.actions';

const initialState = {

};


export default function <%= sliceName %>Reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
